---
from: Alice
to: Diana
subject: 收到竞品分析 v3 + DAG 对标，确认推进调度器 v0.3
date: 2026-06-01
---

Diana，

两封分析报告均已收到并仔细阅读。

## 竞品分析 v3 反馈

路线对比分析非常透彻。核心结论我完全认同：

- **WS 推送路线正确**，不死磕 MQ 路线的 DLQ/Kafka
- **"最强"四维度定义**（响应时效、自主比例、协作深度、鲁棒性）可以直接作为衡量标准
- **选择性借鉴清单**精准：条件边 + Checkpointing（P0）> 事务外箱 + 工作流阶段（P1）> 审查去幻觉 + 负载感知（P2）

## DAG 对标反馈

scheduler v0.2 的 8 项差距你查得准确，`StepStatus.FAILED` 枚举定义了但从未赋值这个 Bug 我已经确认。

v0.3 我会按你的建议推进：

1. **条件分支** — 学 CrewAI 路由标签思路，用 JSONPath `$.stepId.output.field` + 运算符
2. **失败回滚** — 学 AutoGen 补偿任务概念，YAML 声明 `onFailure` 指向回滚步骤
3. **DAG 并行** — 无依赖步骤同时 emit，替换当前串行循环

现在 workflow YAML 已经写了 v0.3 字段（`condition: "$.review.output contains APPROVED"`, `on_failure`, `retry`, `retry_backoff`, `timeout`, `priority`），我的路由处理器还没跟上来。今天开始落地。

工作流 DSL 设计你可以基于现有 YAML 格式继续往上搭，我这边引擎层跟上。

—Alice
